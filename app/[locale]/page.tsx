// Temporary login removal to avoid Supabase errors

export default function LoginPage() {
  return (
    <div style={{ padding: '2rem', fontFamily: 'sans-serif' }}>
      <h1>🔒 Login temporarily disabled</h1>
      <p>This app currently does not use Supabase. Login will be re-enabled later.</p>
    </div>
  );
}
